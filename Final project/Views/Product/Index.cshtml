@using Final_project.Services.SellerPagination
@using Final_project.ViewModel.Seller
@model PagedResult<ProductsVM>

    @{
        ViewData["Title"] = "Products";
}


<partial name="SellerPartialViews/_UpSertModalPartial" />

        


    <a class="btn btn-outline-dark UpSert-btn" href="/Product/UpSert" data-id="">

        <div class="icon icon-shape icon-sm border-radius-md text-center me-2 d-flex align-items-center justify-content-center">
            <i class="ni ni-key-25 text-dark text-sm opacity-10"></i>
        </div>
        <span class="nav-link-text ms-1">Add Product</span>
    </a>
 

     @* products table *@
<div class="row">
    <div class="col-12">
        <div class="card mb-4">
            <div class="card-header pb-0">
                <h6>Products</h6>
            </div>
            <div class="card-body px-0 pt-0 pb-2">
                <div class="table-responsive p-0">
                    <table class="table align-items-center mb-0">
                        <thead>
                            <tr>
                                <th class="text-uppercase text-secondary text-xxs font-weight-bolder opacity-7">Product</th>
                                <th class="text-center text-uppercase text-secondary text-xxs font-weight-bolder opacity-7">Category</th>
                                <th class="text-uppercase text-secondary text-xxs font-weight-bolder opacity-7 ps-2">Price</th>
                                <th class="text-center text-uppercase text-secondary text-xxs font-weight-bolder opacity-7">Brand</th>
                                <th class="text-center text-uppercase text-secondary text-xxs font-weight-bolder opacity-7">Stock_Quantity</th>
                                <th class="text-center text-uppercase text-secondary text-xxs font-weight-bolder opacity-7">CreatedAt</th>
                                <th class="text-center text-uppercase text-secondary text-xxs font-weight-bolder opacity-7">ApprovedBy</th>
                                <th class="text-secondary opacity-7"></th>
                                <th class="text-secondary opacity-7"></th>
                            </tr>
                        </thead>
                        <tbody>
                             @foreach (var item in Model.Items)
                            {
                                <tr>
                                    <td>
                                        <div class="d-flex px-2 py-1">
                                            <div>
                                                <img src="/@item.image_url " class=" rounded-5 w-50 me-3" alt="product">
                                            </div>
                                            <div class="d-flex flex-column justify-content-center">
                                                <h6 class="mb-0 text-sm">@item.name</h6>
                                                <p class="text-xs text-secondary mb-0">ID: @item.id</p>
                                            </div>
                                        </div>
                                    </td>
                                    <td>
                                        <p class="text-xs font-weight-bold mb-0">@item.Category.name</p>
                                    </td>
                                    <td class="align-middle text-center text-sm">
                                        <span class="text-secondary text-xs font-weight-bold">@item.price &pound;</span>
                                    </td>
                                    <td class="align-middle text-center">
                                        <span class="text-secondary text-xs font-weight-bold">@item.Brand</span>
                                    </td>
                                    <td class="align-middle text-center">
                                        <span class="text-secondary text-xs font-weight-bold">@item.stock_quantity</span>
                                    </td>
                                    <td class="align-middle text-center">
                                        <span class="text-secondary text-xs font-weight-bold">@item.created_at</span>
                                    </td>
                                    <td class="align-middle text-center">
                                        <span class="text-secondary text-xs font-weight-bold">@item.approved_by?</span>
                                    </td>
                                    <td class="align-middle btn btn-primary">
                                        <a class="text-secondary font-weight-bold text-xs page-link" href="/instructor/details/@item.id">Details</a>
                                    </td>
                                    <td class="align-middle btn btn-dark ms-1">
                                        <a class="UpSert-Btn text-secondary font-weight-bold text-xs page-link" href="#" data-id="@item.id">Edit</a>
                                    </td>
                                    <td class="align-middle btn btn-danger ms-1">
                                        <button class="delete-btn text-secondary font-weight-bold text-sm page-link" data-id="@item.id">Delete</button>
                                    </td>
                                </tr>

                            } 
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>






    <script src="/lib/jquery/dist/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>


    <script>
    window.addEventListener('load', () => {

        //delete confirmation
        document.querySelectorAll('.delete-btn').forEach(btn => {
            btn.addEventListener('click', () => {

                const Id= btn.getAttribute("data-id");

                    Swal.fire({
                      title: "Are you sure?",
                      text: "You won't be able to revert this!",
                      icon: "warning",
                      showCancelButton: true,
                      confirmButtonColor: "#3085d6",
                      cancelButtonColor: "#d33",
                      confirmButtonText: "Yes, delete it!"
                    }).then((result) => {
                      if (result.isConfirmed) {
                           window.location.href = `/User/Delete/${Id}`;
                        Swal.fire({
                          title: "Deleted!",
                          text: "User has been deleted.",
                          icon: "success"
                        });
                      }
                    });
                 });
               });


    });//end of load

    //ajax

    @* //UpSert product
    $(".UpSert-btn").click(function(){
        const id = $(this).data('id')
        $("#modalBody").load(`/Product/UpSert?id=${id}`,function(response,status,xhr){
            if(status=="error"){
                alert(response);
            }
            else{
                new bootstrap.Modal('#upsertModal').show();
            }

        })
    }) *@


    //sort warehouses

         $(document).on('click', '.sort', function(e){
        e.preventDefault();
        // const sortOrder = $(this).data('sortorder');
        console.log(sortOrder);

        $.ajax({
            url: `/WareHouse/sortTable?sortOrder=${sortOrder}`,
            success: function(result){
                $("#injectTable").html(result);
            },
            error: function () {
                alert("Failed to sort.");
            }
        });
    });



    </script>

<partial name="SellerPartialViews/_PaginationControls" />

